.loader 
  position: absolute
  top: 0
  left:0
  bottom: 0
  right: 0
  margin: auto
  width: 100px
  height: 100px
  animation: rotate 800ms linear infinite

@keyframes rotate {
  0from {
    transform: rotateZ(0deg);
  }

  to {
    transform: rotateZ(360deg);
  }
}
